import { data } from 'persons';

const cards = document.querySelector(".card-gallery");

function generateCard() {
    return `
    <div></div>
    `
}

// const html = generateCard.join("");

cards.innerHTML = html;